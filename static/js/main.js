$(function() {

    // const bsOffcanvas = new bootstrap.Offcanvas('#menu')

    // let flatness = true;

    // $('#switch_flatness').on('change.bootstrapSwitch', function (event, state) {
    //     flatness = event.target.checked
    // }); 

    // -------------------------------------check switches
    // function checkSwitch(id, val){
    //     console.log("The " + id +" is " + val);
    // };

    // var x = document.querySelectorAll(".form-check-input");

    // x.forEach(function(radio) {
    //     radio.addEventListener('change', function(event, state) {
    //         var id = event.target.id;
    //         var val = event.target.checked;
    //         checkSwitch(id, val);
    //     });
    // });

    // //---- init table sorting
    let qctable = new DataTable('#qctable');
    let qctable_new = new DataTable('#qctable_new');
    let pcbs_table = new DataTable('#pcbs_table');
    let modules_table = new DataTable('#modules_main_table');


});